source "https://rubygems.org"

# GitHub Pages compatible set. 228 is also compatible with the Ruby 2.6
# runtime commonly available on macOS for local previews.
gem "github-pages", "= 228", group: :jekyll_plugins
# Ruby 2.6 cannot use the newest ffi release.
gem "ffi", "< 1.17"
